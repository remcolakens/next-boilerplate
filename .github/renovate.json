{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"extends": [
		"config:recommended",
		"group:allNonMajor",
		":semanticCommitTypeAll(chore)",
		"schedule:weekly"
	],
	"labels": ["dependencies"],
	"rangeStrategy": "bump",
	"minimumReleaseAge": "3 days",
	"timezone": "Europe/Amsterdam",
	"packageRules": [
		{
			"description": "Automatically merge minor and patch-level updates",
			"matchUpdateTypes": ["minor", "patch", "digest"],
			"automerge": true
		},
		{
			"description": "mcr.microsoft.com/playwright tags don't reliably expose a release timestamp, which was silently blocking updates under minimumReleaseAge and letting it drift out of sync with @playwright/test",
			"matchDatasources": ["docker"],
			"matchPackageNames": ["mcr.microsoft.com/playwright"],
			"minimumReleaseAge": "0 days"
		}
	],
	"ignoreDeps": ["node"]
}
